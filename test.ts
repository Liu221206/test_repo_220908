




const getRawSkillTarget = (
  id: string
): SkillTarget | undefined => {
  return rawSkillTarget.find(it => it.id === id)
}

function getRawSkillTarget(
  id: string
): SkillTarget | undefined {
  return rawSkillTarget.find(it => it.id === id)
}
